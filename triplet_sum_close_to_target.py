import math

def triplet_sum_close_to_target(arr, target_sum):
    n = len(arr)
    left = 0
    right = n-1
    smallest_diff = math.inf
    # triplet = []
    arr.sort()
    for i in range(len(arr)):
        left = i
        right = 
    

